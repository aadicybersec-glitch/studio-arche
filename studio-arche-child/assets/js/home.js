/**
 * Studio Arche — Home Page JS
 */
(function() {
    'use strict';
    // Core engine handles the majority of the data-attribute based animations.
    // We can add specific GSAP timelines here if needed.
    
    document.addEventListener('DOMContentLoaded', () => {
        if (!window.gsap) return;
        // Example of a specific home page timeline if required later.
    });
})();
